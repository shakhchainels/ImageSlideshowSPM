import XCTest
@testable import ImageSlideshowSPM

final class ImageSlideshowSPMTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ImageSlideshowSPM().text, "Hello, World!")
    }
}
